# Pass 2 — Figures & Tables

### Figure 1 — Overall pre-training and fine-tuning procedures for BERT.

Shows the MLM masking positions, the NSP pair construction, and the task-specific output layers for downstream tasks (classification, span start/end, etc.).

### Figure 2 — BERT input representation.

Shows token embeddings + segment embeddings + position embeddings, summed.

### Figure 3 — Fine-tuning on five downstream tasks.

CoLA, SST-2, MNLI, QQP, SQuAD — shows how BERT is fine-tuned for each task family.

### Table 1 — GLUE dev/test results.

BERT_LARGE 80.5% on GLUE test set, 7.7% absolute improvement over prior state of the art.

### Table 2 — SQuAD v1.1 and v2.0 results.

BERT_LARGE Test F1 93.2 on SQuAD v1.1 and 83.1 on SQuAD v2.0.

### Table 3 — SWAG results.

BERT_LARGE 86.6% accuracy on SWAG commonsense inference.

### Table 4 — Ablation studies.

Effect of feature-based vs fine-tuning, MLM vs LTR, model size, and training data on downstream performance.

